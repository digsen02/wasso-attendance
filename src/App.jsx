import { useMemo, useRef, useState } from 'react';
import { Navigate, NavLink, Route, Routes, useLocation, useNavigate } from 'react-router-dom';
import { ArrowLeft, ArrowRight, BarChart3, Bell, Building2, CalendarDays, CheckCircle2, ChevronDown, ChevronRight, CircleAlert, Clock3, Download, Eye, EyeOff, FileText, LayoutDashboard, LogOut, Menu, Search, Send, UploadCloud, User, UserPlus, Users, X } from 'lucide-react';
import { useApp } from './context/AppContext';
import { AttendanceStatus, ReportStatus, Role } from './data/seed';
import { dataSource } from './repositories';
import { formatDateTime, getDday, statusMeta } from './utils/format';

const currentMonth='2026-08';
function Logo(){return <div className="logo"><strong>회사와쏘</strong><span>FIELD PRACTICE ATTENDANCE SYSTEM</span></div>}
function Button({children,variant='primary',icon:Icon,...props}){return <button className={`btn ${variant}`} {...props}>{Icon&&<Icon size={17}/>}<span>{children}</span></button>}
function Status({value}){const meta=statusMeta[value]||{label:value,tone:'neutral'};return <span className={`status ${meta.tone}`}><CheckCircle2 size={11}/>{meta.label}</span>}
function Footer(){return <footer><div><b>회사와쏘</b> FIELD PRACTICE ATTENDANCE SYSTEM<br/><span>© 2026 HOESAWASSO. ALL RIGHTS RESERVED.</span></div><div className="footer-links"><span>이용약관</span><span>개인정보처리방침</span><span>문의하기</span></div></footer>}
function Toast(){const {state}=useApp();return state.toast?<div className={`toast ${state.toast.kind}`}><CheckCircle2 size={18}/>{state.toast.message}</div>:null}
function Loading(){return <div className="loading-screen"><span className="spinner"/><b>데이터를 불러오고 있습니다.</b></div>}
function Empty({title='표시할 데이터가 없습니다.',description}){return <div className="empty-state"><FileText/><b>{title}</b>{description&&<p>{description}</p>}</div>}

function Protected({role,children}){const {state}=useApp();const location=useLocation();if(!state.ready)return <Loading/>;if(!state.currentUser)return <Navigate to="/login" replace state={{from:location}}/>;if(role&&state.currentUser.role!==role)return <Navigate to={state.currentUser.role===Role.ADMIN?'/admin/dashboard':'/student'} replace/>;return children}

function NotificationPanel({onClose}){const {state,actions}=useApp();const items=state.notifications.filter(n=>n.userId===state.currentUser.id);return <div className="notification-panel"><div className="panel-head"><h3>알림</h3><button aria-label="알림 닫기" onClick={onClose}><X/></button></div>{items.length?items.map(n=><button key={n.id} className={`notification-item ${n.isRead?'':'unread'}`} onClick={()=>actions.markRead(n.id)}><span><Bell/></span><div><b>{statusMeta[n.type]?.label||n.type}</b><p>{n.message}</p><small>{formatDateTime(n.createdAt)}</small></div>{!n.isRead&&<i/>}</button>):<Empty title="새로운 알림이 없습니다."/>}</div>}

function Shell({role,children}){const {state,actions}=useApp();const [menu,setMenu]=useState(false),[notices,setNotices]=useState(false);const navigate=useNavigate();const nav=role===Role.STUDENT?[["/student","홈",LayoutDashboard],["/student/attendance","출근부",CalendarDays],["/student/profile","내 정보",User]]:[["/admin/dashboard","대시보드",LayoutDashboard],["/admin/students","학생 관리",Users],["/admin/attendance","출근부 관리",CalendarDays]];const unread=state.notifications.filter(n=>n.userId===state.currentUser.id&&!n.isRead).length;const logout=async()=>{await actions.logout();navigate('/login')};return <div className="app-page"><header className="app-header"><Logo/><nav>{nav.map(([to,label])=><NavLink key={to} end={to==='/student'} to={to}>{label}</NavLink>)}</nav><div className="account"><button className="bell-button" aria-label="알림" onClick={()=>setNotices(!notices)}><Bell/>{unread>0&&<i>{unread}</i>}</button><span><b>{state.currentUser.name}</b><small>{role}</small></span><button aria-label="로그아웃" onClick={logout}><LogOut/></button></div><button className="mobile-menu" aria-label="메뉴" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></header>{menu&&<div className="mobile-nav">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setMenu(false)}><Icon/>{label}</NavLink>)}<button onClick={logout}><LogOut/>로그아웃</button></div>}{notices&&<NotificationPanel onClose={()=>setNotices(false)}/>}<main className="app-body">{children}</main><Footer/><Toast/></div>}

function AuthLayout({children}){return <div className="page auth-page"><header className="auth-header"><Logo/><span>FOR STUDENTS & ADMINISTRATORS</span><span className="support">고객센터 02-1234-5678<br/>HELP@HOESAWASSO.KR</span></header><section className="auth-hero"><h1>회사와쏘</h1><p>FIELD PRACTICE ATTENDANCE SYSTEM</p></section>{children}<Footer/><Toast/></div>}
function Login(){const {state,actions}=useApp();const [role,setRole]=useState(Role.STUDENT),[username,setUsername]=useState('student'),[password,setPassword]=useState('password'),[show,setShow]=useState(false);const navigate=useNavigate();if(state.ready&&state.currentUser)return <Navigate to={state.currentUser.role===Role.ADMIN?'/admin/dashboard':'/student'} replace/>;const choose=r=>{setRole(r);setUsername(r===Role.ADMIN?'admin':'student')};const submit=async e=>{e.preventDefault();try{const user=await actions.login(username,password);navigate(user.role===Role.ADMIN?'/admin/dashboard':'/student',{replace:true})}catch{}};return <AuthLayout><main className="login-grid"><aside className="auth-copy"><p className="eyebrow">MONTHLY ATTENDANCE</p><h2>이번 달 출근부를<br/>더 간단하게 관리합니다</h2><p>현장실습생과 담당 관리자가 출근부를 쉽고 정확하게 관리할 수 있도록 돕는 서비스입니다.</p><div className="feature-list"><div><CalendarDays/><span><b>간편한 출근 체크</b>매일의 근무 기록 관리</span></div><div><BarChart3/><span><b>실시간 제출 현황</b>계산된 통계를 한눈에 확인</span></div><div><FileText/><span><b>안전한 PDF 제출</b>검증부터 알림까지</span></div></div></aside><form className="login-form" onSubmit={submit}><div className="role-tabs"><button type="button" className={role===Role.STUDENT?'active':''} onClick={()=>choose(Role.STUDENT)}>학생 로그인</button><button type="button" className={role===Role.ADMIN?'active':''} onClick={()=>choose(Role.ADMIN)}>관리자 로그인</button></div><div className="form-head"><span className="eyebrow">{dataSource} DATA SOURCE</span><h3>{role===Role.STUDENT?'학생 계정으로 로그인':'관리자 계정으로 로그인'}</h3><p>데모 계정: {role===Role.STUDENT?'student':'admin'} / password</p></div><label>아이디<input value={username} onChange={e=>setUsername(e.target.value)} required/></label><label>비밀번호<div className="input-icon"><input value={password} onChange={e=>setPassword(e.target.value)} required type={show?'text':'password'}/><button type="button" aria-label="비밀번호 보기" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{state.error&&<div className="form-error"><CircleAlert/>{state.error}</div>}<Button type="submit" disabled={state.loading}>{state.loading?'로그인 중...':role===Role.STUDENT?'학생으로 시작하기':'관리자로 시작하기'}<ArrowRight/></Button><div className="signup-link">계정이 없으신가요? <NavLink to="/signup">학생 회원가입</NavLink></div></form><aside className="schedule"><p className="eyebrow">THIS MONTH</p><h3>이번 달 주요 일정</h3><CalendarDays/><b>출근부 제출 마감일</b><strong>2026. 08. 31</strong><span>23:59까지 제출해주세요.</span><div className="note"><CircleAlert/><b>안내사항</b><p>마감 후 제출은 인정되지 않을 수 있습니다.</p></div></aside></main></AuthLayout>}
function Signup(){const {actions}=useApp();const navigate=useNavigate();const submit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));try{await actions.signup(data);navigate('/login')}catch{}};return <AuthLayout><main className="signup-wrap"><aside><NavLink className="back-link" to="/login"><ArrowLeft/>로그인으로</NavLink><p className="eyebrow">STUDENT ONLY</p><h1>학생 회원가입</h1><p>학생 정보와 실제 현장실습 정보를 정확히 입력해주세요.</p></aside><form className="signup-form" onSubmit={submit}><h2>회원 정보를 입력하세요.</h2><div className="two-col">{[['username','아이디','student2'],['password','비밀번호','8자 이상'],['studentNumber','학번','2023999'],['name','이름','홍길동'],['school','학교','학교명'],['classNumber','반','4반'],['company','실습 회사','회사명']].map(([name,label,placeholder])=><label key={name}>{label}<input name={name} type={name==='password'?'password':'text'} placeholder={placeholder} required/></label>)}<label>실습 시작일<input name="startDate" type="date" required/></label><label>실습 종료일<input name="endDate" type="date" required/></label></div><label className="check agree"><input type="checkbox" required/>이용약관과 개인정보처리방침에 동의합니다.</label><Button type="submit">회원가입</Button></form></main></AuthLayout>}

function PageHead({eyebrow,title,description,children}){return <div className="page-heading"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{description}</p></div>{children}</div>}
function StudentHome(){const {state}=useApp();const navigate=useNavigate();const report=state.reports.find(r=>r.studentId===state.currentUser.id&&r.yearMonth===currentMonth);const logs=state.logs.filter(l=>l.studentId===state.currentUser.id&&l.date.startsWith(currentMonth));const period=state.periods.find(p=>p.yearMonth===currentMonth);return <Shell role={Role.STUDENT}><div className="student-home"><section className="hero-dashboard"><div><p className="eyebrow">AUGUST 2026</p><h1>이번 달 출근부,<br/>제출하셨나요?</h1><p>매월 정해진 기간 내에 출근부를 제출해야 정상 근태로 인정됩니다.</p></div><div className="month-card"><div className="card-title"><div><h2>8월 출근부 <Status value={report?.status||ReportStatus.NOT_STARTED}/></h2><p>{report?.status===ReportStatus.SUBMITTED?'제출이 완료되었습니다.':'마감일 전까지 출근부를 작성하고 제출해주세요.'}</p></div><span className="d-day">D-{getDday(period)}</span></div><div className="metric-row"><div><CalendarDays/><span><small>마감일</small><b>2026. 08. 31</b></span></div><div><BarChart3/><span><small>근무 기록</small><b>{logs.length}일</b></span></div><div><FileText/><span><small>PDF 상태</small><b>{report?.fileName?'업로드':'미업로드'}</b></span></div><div><Clock3/><span><small>제출 일시</small><b>{formatDateTime(report?.submittedAt)}</b></span></div></div><div className="action-row"><Button onClick={()=>navigate('/student/attendance')}>출근부 작성 <ArrowRight/></Button><Button variant="outline" onClick={()=>navigate('/student/profile')}>내 정보 <ChevronRight/></Button></div></div></section><section className="dashboard-grid"><div className="panel notifications"><div className="panel-head"><h3>최근 알림</h3></div>{state.notifications.filter(n=>n.userId===state.currentUser.id).slice(0,3).map(n=><div className="notice" key={n.id}><span><Bell/></span><div><b>{n.message}</b><p>{formatDateTime(n.createdAt)}</p></div>{!n.isRead&&<i className="unread-dot"/>}</div>)}</div><div className="panel company-panel"><div className="panel-head"><h3>인턴십 정보</h3></div><div className="company-content"><dl><dt>기업명</dt><dd>{state.currentUser.company}</dd><dt>학교</dt><dd>{state.currentUser.school}</dd><dt>실습 기간</dt><dd>{state.currentUser.startDate} – {state.currentUser.endDate}</dd></dl><div className="office-art"><Building2/><span>FIELD<br/>STUDIO</span></div></div></div></section></div></Shell>}

function Attendance(){const {state,actions}=useApp();const user=state.currentUser;const report=state.reports.find(r=>r.studentId===user.id&&r.yearMonth===currentMonth);const source=state.logs.filter(l=>l.studentId===user.id&&l.date.startsWith(currentMonth));const [logs,setLogs]=useState(source);const input=useRef();const readOnly=[ReportStatus.SUBMITTED,ReportStatus.APPROVED].includes(report?.status);const change=(id,key,value)=>setLogs(v=>v.map(l=>l.id===id?{...l,[key]:value}:l));const upload=async file=>{try{await actions.uploadPdf(report,file)}catch{if(input.current)input.current.value=''}};if(!report)return <Shell role={Role.STUDENT}><div className="workspace"><Empty title="이번 달 리포트가 없습니다."/></div></Shell>;return <Shell role={Role.STUDENT}><div className="workspace"><PageHead eyebrow="MY ATTENDANCE" title="월별 출근부" description="근무 기록을 수정하고 PDF 출근부를 제출하세요."><div className="date-box"><CalendarDays/>2026. 08</div></PageHead><div className="submission-period"><CalendarDays/><div><b>제출 기간</b><span>2026.08.01 – 08.31 23:59</span></div><strong>D-{getDday(state.periods.find(p=>p.yearMonth===currentMonth))}</strong></div><div className="table-wrap attendance-table"><table><thead><tr><th>날짜</th><th>상태</th><th>출근</th><th>퇴근</th><th>근무 내용</th><th>비고</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{log.date}</td><td><select disabled={readOnly} value={log.status} onChange={e=>change(log.id,'status',e.target.value)}>{Object.values(AttendanceStatus).map(s=><option key={s}>{s}</option>)}</select></td><td><input aria-label={`${log.date} 출근`} disabled={readOnly} type="time" value={log.checkIn||''} onChange={e=>change(log.id,'checkIn',e.target.value)}/></td><td><input aria-label={`${log.date} 퇴근`} disabled={readOnly} type="time" value={log.checkOut||''} onChange={e=>change(log.id,'checkOut',e.target.value)}/></td><td><input aria-label={`${log.date} 근무 내용`} disabled={readOnly} value={log.workSummary} onChange={e=>change(log.id,'workSummary',e.target.value)}/></td><td><input aria-label={`${log.date} 비고`} disabled={readOnly} value={log.note||''} onChange={e=>change(log.id,'note',e.target.value)}/></td></tr>)}</tbody></table></div><div className="submission-grid"><section className="panel upload-panel"><div className="panel-head"><h3>PDF 업로드</h3><span>PDF · 최대 10MB</span></div><input ref={input} id="pdf-upload" type="file" accept="application/pdf" hidden disabled={readOnly} onChange={e=>upload(e.target.files[0])}/><label htmlFor="pdf-upload" className={`dropzone ${readOnly?'disabled':''}`} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();if(!readOnly)upload(e.dataTransfer.files[0])}}><UploadCloud/><b>클릭하거나 PDF를 끌어다 놓으세요.</b><span>학생 정보 기반 파일명으로 자동 변경됩니다.</span></label>{report.fileName&&<div className="uploaded-file"><FileText/><span><b>{report.fileName}</b><small>자동 생성 파일명</small></span><CheckCircle2/></div>}</section><section className="panel submit-panel"><div className="panel-head"><h3>제출 정보</h3><Status value={report.status}/></div><dl><dt>마감일</dt><dd>2026.08.31</dd><dt>제출 일시</dt><dd>{formatDateTime(report.submittedAt)}</dd><dt>편집 상태</dt><dd>{readOnly?'읽기 전용':'편집 가능'}</dd></dl><div className="action-row"><Button variant="outline" disabled={readOnly||state.loading} onClick={()=>actions.saveDraft(report,logs)}>임시 저장</Button><Button disabled={readOnly||!report.filePath||state.loading} onClick={()=>actions.submitReport(report,logs)}>최종 제출</Button></div></section></div></div></Shell>}

function Profile(){const {state,actions}=useApp();const u=state.currentUser;const navigate=useNavigate();return <Shell role={Role.STUDENT}><div className="workspace profile"><PageHead eyebrow="MY PROFILE" title="학생 프로필" description="내 정보와 현장실습 정보를 확인합니다."/><section className="profile-card"><div className="avatar">{u.name.slice(0,1)}</div><div><h2>{u.name} <Status value="APPROVED"/></h2><p>{u.school} · {u.classNumber}</p></div></section><div className="profile-grid"><section className="panel"><h3>기본 정보</h3><dl className="info-list"><dt>학번</dt><dd>{u.studentNumber}</dd><dt>아이디</dt><dd>{u.username}</dd><dt>역할</dt><dd>{u.role}</dd></dl></section><section className="panel"><h3>현장실습 정보</h3><dl className="info-list"><dt>회사</dt><dd>{u.company}</dd><dt>시작일</dt><dd>{u.startDate}</dd><dt>종료일</dt><dd>{u.endDate}</dd></dl></section></div><Button variant="danger" icon={LogOut} onClick={async()=>{await actions.logout();navigate('/login')}}>로그아웃</Button></div></Shell>}

const submittedStatuses=[ReportStatus.SUBMITTED,ReportStatus.APPROVED];
function useAdminRows(){const {state}=useApp();return state.users.filter(u=>u.role===Role.STUDENT).map(user=>({user,report:state.reports.find(r=>r.studentId===user.id&&r.yearMonth===currentMonth),logs:state.logs.filter(l=>l.studentId===user.id&&l.date.startsWith(currentMonth))}))}
function Metric({label,value,suffix,Icon}){return <div className="metric"><div><span>{label}</span><strong>{value}<small>{suffix}</small></strong></div><Icon/></div>}
function StudentTable({rows,onSelect}){return <div className="table-wrap"><table><thead><tr><th>학번</th><th>이름</th><th>반</th><th>학교</th><th>회사</th><th>상태</th><th>제출 일시</th></tr></thead><tbody>{rows.map(({user,report})=><tr key={user.id} onClick={()=>onSelect?.({user,report})}><td>{user.studentNumber}</td><td>{user.name}</td><td>{user.classNumber}</td><td>{user.school}</td><td>{user.company}</td><td><Status value={report?.status||ReportStatus.NOT_STARTED}/></td><td>{formatDateTime(report?.submittedAt)}</td></tr>)}</tbody></table></div>}
function AdminDashboard(){const rows=useAdminRows();const {actions}=useApp();const [selected,setSelected]=useState(null);const completed=rows.filter(r=>submittedStatuses.includes(r.report?.status)).length;const missing=rows.filter(r=>!submittedStatuses.includes(r.report?.status));const classes=Object.entries(rows.reduce((a,r)=>{const k=r.user.classNumber;(a[k]??={total:0,done:0}).total++;if(submittedStatuses.includes(r.report?.status))a[k].done++;return a},{}));return <Shell role={Role.ADMIN}><div className="workspace admin-dashboard"><PageHead eyebrow="ADMINISTRATION" title="제출 현황 대시보드" description="실제 리포트 데이터로 계산된 제출 현황입니다."><div className="date-box"><CalendarDays/>2026.08</div></PageHead><div className="metrics"><Metric label="전체 학생" value={rows.length} suffix="명" Icon={Users}/><Metric label="제출 완료" value={completed} suffix="명" Icon={CheckCircle2}/><Metric label="미제출" value={rows.length-completed} suffix="명" Icon={CircleAlert}/><Metric label="제출률" value={rows.length?((completed/rows.length)*100).toFixed(1):0} suffix="%" Icon={BarChart3}/></div><div className="admin-grid"><section className="panel"><div className="panel-head"><h3>학생 제출 현황</h3><NavLink to="/admin/students">전체 보기 <ChevronRight/></NavLink></div>{rows.length?<StudentTable rows={rows} onSelect={setSelected}/>:<Empty/>}</section><aside className="admin-side"><section className="panel"><h3>반별 제출률</h3>{classes.map(([name,v])=>{const rate=Math.round(v.done/v.total*100);return <div className="progress-item" key={name}><div><b>{name}</b><span>{rate}% ({v.done}/{v.total})</span></div><div className="progress"><i style={{width:`${rate}%`}}/></div></div>})}</section><section className="panel no-submit"><div className="panel-head"><h3>미제출자 ({missing.length}명)</h3></div>{missing.map(r=><div key={r.user.id}><span>{r.user.name}<small>{r.user.studentNumber} · {r.user.classNumber}</small></span><button aria-label={`${r.user.name} Reminder 재발송`} onClick={()=>actions.sendReminder(r.user)}><Send/></button></div>)}</section></aside></div>{selected&&<StudentModal data={selected} onClose={()=>setSelected(null)}/>}</div></Shell>}

function AdminStudents(){const rows=useAdminRows();const [query,setQuery]=useState(''),[status,setStatus]=useState('ALL'),[classNo,setClassNo]=useState('ALL'),[selected,setSelected]=useState(null);const filtered=rows.filter(r=>(!query||`${r.user.name} ${r.user.studentNumber}`.includes(query))&&(status==='ALL'||(status==='DONE'?submittedStatuses.includes(r.report?.status):!submittedStatuses.includes(r.report?.status)))&&(classNo==='ALL'||r.user.classNumber===classNo));return <Shell role={Role.ADMIN}><div className="workspace"><PageHead eyebrow="STUDENT DIRECTORY" title="학생 관리" description="학생 검색과 제출 상태 필터를 이용하세요."><Button icon={UserPlus}>학생 추가</Button></PageHead><section className="filter-bar"><div className="segmented">{[['ALL','전체'],['DONE','제출'],['MISSING','미제출']].map(([v,l])=><button className={status===v?'active':''} onClick={()=>setStatus(v)} key={v}>{l}</button>)}</div><label className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="이름 또는 학번 검색"/></label><select aria-label="반 필터" value={classNo} onChange={e=>setClassNo(e.target.value)}><option value="ALL">전체 반</option>{[...new Set(rows.map(r=>r.user.classNumber))].map(v=><option key={v}>{v}</option>)}</select></section><section className="panel student-list"><div className="panel-head"><h3>검색 결과 <span>{filtered.length}명</span></h3></div>{filtered.length?<StudentTable rows={filtered} onSelect={setSelected}/>:<Empty title="조건에 맞는 학생이 없습니다."/>}</section>{selected&&<StudentModal data={selected} onClose={()=>setSelected(null)}/>}</div></Shell>}
function AdminAttendance(){const rows=useAdminRows();const [selected,setSelected]=useState(null);return <Shell role={Role.ADMIN}><div className="workspace"><PageHead eyebrow="REVIEW ATTENDANCE" title="출근부 관리" description="제출된 출근부를 검토하고 승인 또는 반려합니다."/><section className="filter-bar"><div className="date-box"><CalendarDays/>2026.08</div><span className="filter-result">총 {rows.length}건 · 검토 대기 {rows.filter(r=>r.report?.status===ReportStatus.SUBMITTED).length}건</span></section><section className="panel student-list"><StudentTable rows={rows} onSelect={setSelected}/></section>{selected&&<StudentModal data={selected} onClose={()=>setSelected(null)}/>}</div></Shell>}
function StudentModal({data,onClose}){const {state,actions}=useApp();const report=state.reports.find(r=>r.studentId===data.user.id&&r.yearMonth===currentMonth);const logs=state.logs.filter(l=>l.studentId===data.user.id&&l.date.startsWith(currentMonth));const canReview=report?.status===ReportStatus.SUBMITTED;const download=async()=>{const url=await actions.getReportUrl(report);window.open(url,'_blank','noopener,noreferrer')};return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><div className="modal-head"><div><p className="eyebrow">SUBMISSION DETAIL</p><h2>출근부 제출 상세</h2></div><button aria-label="닫기" onClick={onClose}><X/></button></div><div className="detail-top"><section><h3>학생 정보</h3><dl><dt>이름</dt><dd>{data.user.name}</dd><dt>학번</dt><dd>{data.user.studentNumber}</dd><dt>학교</dt><dd>{data.user.school}</dd><dt>반</dt><dd>{data.user.classNumber}</dd></dl></section><section><h3>실습·제출 정보</h3><dl><dt>회사</dt><dd>{data.user.company}</dd><dt>기간</dt><dd>{data.user.startDate} – {data.user.endDate}</dd><dt>제출 일시</dt><dd>{formatDateTime(report?.submittedAt)}</dd><dt>상태</dt><dd><Status value={report?.status||ReportStatus.NOT_STARTED}/></dd></dl></section></div><section className="detail-att"><div className="panel-head"><h3>일별 출근 기록</h3><span>총 {logs.length}일</span></div>{logs.length?<div className="mini-attendance">{logs.map(l=><div key={l.id}><b>{l.date}</b><span>{l.checkIn||'—'} – {l.checkOut||'—'}</span><span>{l.workSummary}</span></div>)}</div>:<Empty title="출근 기록이 없습니다."/>}</section>{report?.fileName?<div className="file-card"><FileText/><span><b>{report.fileName}</b><small>PDF 제출 파일</small></span><button aria-label="PDF 다운로드" onClick={download}><Download/></button></div>:<Empty title="제출된 PDF가 없습니다."/>}<div className="modal-actions"><Button variant="outline" disabled={!canReview} onClick={()=>actions.reviewReport(report,data.user,ReportStatus.REJECTED)}>반려</Button><Button disabled={!canReview} onClick={()=>actions.reviewReport(report,data.user,ReportStatus.APPROVED)}>승인</Button></div></div></div>}

function NotFound(){return <div className="loading-screen"><CircleAlert/><h2>페이지를 찾을 수 없습니다.</h2><NavLink to="/login">로그인으로 돌아가기</NavLink></div>}
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route path="/signup" element={<Signup/>}/><Route path="/student" element={<Protected role={Role.STUDENT}><StudentHome/></Protected>}/><Route path="/student/attendance" element={<Protected role={Role.STUDENT}><Attendance/></Protected>}/><Route path="/student/profile" element={<Protected role={Role.STUDENT}><Profile/></Protected>}/><Route path="/admin" element={<Navigate to="/admin/dashboard" replace/>}/><Route path="/admin/dashboard" element={<Protected role={Role.ADMIN}><AdminDashboard/></Protected>}/><Route path="/admin/students" element={<Protected role={Role.ADMIN}><AdminStudents/></Protected>}/><Route path="/admin/attendance" element={<Protected role={Role.ADMIN}><AdminAttendance/></Protected>}/><Route path="/" element={<Navigate to="/login" replace/>}/><Route path="*" element={<NotFound/>}/></Routes>}
